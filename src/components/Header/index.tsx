"use client";

export const Header = () => {
  // const { data } = useGetUser();

  // console.log(data);
  return (
    <header className="flex items-center justify-between ring-1">
      <h2>Lucas Calixto</h2>
      imagem
    </header>
  );
};
